# Git Setup Log — [Naef]
**Program:** AI.SPIRE Pre-Work
**Date completed:** [2002-02-23]
**GitHub username:** [Naefadwan]

---

## Git Identity

Run `git config --list` in your terminal and fill in your configured values:

| Config key | Your value |
|------------|------------|
| `user.name` |Naefadwan |
| `user.email` |Naef21@proton.me |
| `core.autocrlf` | true|

---

## Platform

| Field | Your value |
|-------|------------|
| Operating system | (Ubuntu 22.04) |
| Terminal used | ( bash on Ubuntu) |
| Git version (`git version 2.51.0`) | |

---

## Authentication

- [ ] Personal Access Token created in GitHub Settings
- [1 ] PAT stored securely (password manager or secure notes)
- [ ] Credential caching confirmed — Git does not re-prompt on push

---

## Clone Verification

After cloning, output of `git status` from inside your repo:
